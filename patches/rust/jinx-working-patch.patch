diff --git rust-clean/compiler/rustc_target/src/spec/base/mod.rs rust-workdir/compiler/rustc_target/src/spec/base/mod.rs
index 9e7ff62..7faabc6 100644
--- rust-clean/compiler/rustc_target/src/spec/base/mod.rs
+++ rust-workdir/compiler/rustc_target/src/spec/base/mod.rs
@@ -23,6 +23,7 @@ pub(crate) mod linux_uclibc;
 pub(crate) mod linux_wasm;
 pub(crate) mod lynxos178;
 pub(crate) mod managarm_mlibc;
+pub(crate) mod zinnia_mlibc;
 pub(crate) mod motor;
 pub(crate) mod msvc;
 pub(crate) mod netbsd;
diff --git rust-workdir/compiler/rustc_target/src/spec/base/zinnia_mlibc.rs rust-workdir/compiler/rustc_target/src/spec/base/zinnia_mlibc.rs
new file mode 100644
index 0000000..c258ec1
--- /dev/null
+++ rust-workdir/compiler/rustc_target/src/spec/base/zinnia_mlibc.rs
@@ -0,0 +1,17 @@
+use crate::spec::{Env, Os, RelroLevel, TargetOptions, cvs};
+
+pub(crate) fn opts() -> TargetOptions {
+    TargetOptions {
+        os: Os::Zinnia,
+        env: Env::Mlibc,
+        dynamic_linking: true,
+        executables: true,
+        families: cvs!["unix"],
+        has_rpath: true,
+        position_independent_executables: true,
+        relro_level: RelroLevel::Full,
+        has_thread_local: true,
+        crt_static_respected: true,
+        ..Default::default()
+    }
+}
diff --git rust-clean/compiler/rustc_target/src/spec/mod.rs rust-workdir/compiler/rustc_target/src/spec/mod.rs
index d9c872c..1f1e4cf 100644
--- rust-clean/compiler/rustc_target/src/spec/mod.rs
+++ rust-workdir/compiler/rustc_target/src/spec/mod.rs
@@ -1571,6 +1571,10 @@ supported_targets! {
     ("aarch64-unknown-managarm-mlibc", aarch64_unknown_managarm_mlibc),
     ("riscv64gc-unknown-managarm-mlibc", riscv64gc_unknown_managarm_mlibc),
 
+    ("x86_64-unknown-zinnia-mlibc", x86_64_unknown_zinnia_mlibc),
+    ("aarch64-unknown-zinnia-mlibc", aarch64_unknown_zinnia_mlibc),
+    ("riscv64gc-unknown-zinnia-mlibc", riscv64gc_unknown_zinnia_mlibc),
+
     ("i386-apple-ios", i386_apple_ios),
     ("x86_64-apple-ios", x86_64_apple_ios),
     ("aarch64-apple-ios", aarch64_apple_ios),
@@ -1982,6 +1986,7 @@ crate::target_spec_enum! {
         LynxOs178 = "lynxos178",
         MacOs = "macos",
         Managarm = "managarm",
+        Zinnia = "zinnia",
         Motor = "motor",
         NetBsd = "netbsd",
         None = "none",
diff --git rust-workdir/compiler/rustc_target/src/spec/targets/aarch64_unknown_zinnia_mlibc.rs rust-workdir/compiler/rustc_target/src/spec/targets/aarch64_unknown_zinnia_mlibc.rs
new file mode 100644
index 0000000..7ce135a
--- /dev/null
+++ rust-workdir/compiler/rustc_target/src/spec/targets/aarch64_unknown_zinnia_mlibc.rs
@@ -0,0 +1,22 @@
+use crate::spec::{Arch, StackProbeType, Target, base};
+
+pub(crate) fn target() -> Target {
+    let mut base = base::zinnia_mlibc::opts();
+    base.max_atomic_width = Some(128);
+    base.stack_probes = StackProbeType::Inline;
+    base.features = "+v8a".into();
+
+    Target {
+        llvm_target: "aarch64-unknown-zinnia-mlibc".into(),
+        metadata: crate::spec::TargetMetadata {
+            description: None,
+            tier: Some(3),
+            host_tools: Some(false),
+            std: Some(false),
+        },
+        pointer_width: 64,
+        data_layout: "e-m:e-p270:32:32-p271:32:32-p272:64:64-i8:8:32-i16:16:32-i64:64-i128:128-n32:64-S128-Fn32".into(),
+        arch: Arch::AArch64,
+        options: base
+    }
+}
diff --git rust-workdir/compiler/rustc_target/src/spec/targets/riscv64gc_unknown_zinnia_mlibc.rs rust-workdir/compiler/rustc_target/src/spec/targets/riscv64gc_unknown_zinnia_mlibc.rs
new file mode 100644
index 0000000..3c4ae22
--- /dev/null
+++ rust-workdir/compiler/rustc_target/src/spec/targets/riscv64gc_unknown_zinnia_mlibc.rs
@@ -0,0 +1,24 @@
+use crate::spec::{Arch, CodeModel, Target, TargetOptions, base};
+
+pub(crate) fn target() -> Target {
+    Target {
+        llvm_target: "riscv64-unknown-zinnia-mlibc".into(),
+        metadata: crate::spec::TargetMetadata {
+            description: None,
+            tier: Some(3),
+            host_tools: Some(false),
+            std: Some(false),
+        },
+        pointer_width: 64,
+        data_layout: "e-m:e-p:64:64-i64:64-i128:128-n32:64-S128".into(),
+        arch: Arch::RiscV64,
+        options: TargetOptions {
+            code_model: Some(CodeModel::Medium),
+            cpu: "generic-rv64".into(),
+            features: "+m,+a,+f,+d,+c".into(),
+            llvm_abiname: "lp64d".into(),
+            max_atomic_width: Some(64),
+            ..base::zinnia_mlibc::opts()
+        },
+    }
+}
diff --git rust-workdir/compiler/rustc_target/src/spec/targets/x86_64_unknown_zinnia_mlibc.rs rust-workdir/compiler/rustc_target/src/spec/targets/x86_64_unknown_zinnia_mlibc.rs
new file mode 100644
index 0000000..33e9d26
--- /dev/null
+++ rust-workdir/compiler/rustc_target/src/spec/targets/x86_64_unknown_zinnia_mlibc.rs
@@ -0,0 +1,24 @@
+use crate::spec::{Arch, Cc, LinkerFlavor, Lld, StackProbeType, Target, base};
+
+pub(crate) fn target() -> Target {
+    let mut base = base::zinnia_mlibc::opts();
+    base.cpu = "x86-64".into();
+    base.max_atomic_width = Some(64);
+    base.add_pre_link_args(LinkerFlavor::Gnu(Cc::Yes, Lld::No), &["-m64"]);
+    base.stack_probes = StackProbeType::Inline;
+
+    Target {
+        llvm_target: "x86_64-unknown-zinnia-mlibc".into(),
+        metadata: crate::spec::TargetMetadata {
+            description: None,
+            tier: Some(3),
+            host_tools: Some(false),
+            std: Some(false),
+        },
+        pointer_width: 64,
+        data_layout:
+            "e-m:e-p270:32:32-p271:32:32-p272:64:64-i64:64-i128:128-f80:128-n8:16:32:64-S128".into(),
+        arch: Arch::X86_64,
+        options: base,
+    }
+}
diff --git rust-clean/library/std/build.rs rust-workdir/library/std/build.rs
index c0a6e30..1e07e73 100644
--- rust-clean/library/std/build.rs
+++ rust-workdir/library/std/build.rs
@@ -57,6 +57,7 @@ fn main() {
         || target_os == "nuttx"
         || target_os == "cygwin"
         || target_os == "vexos"
+        || target_os == "zinnia"
 
         // See src/bootstrap/src/core/build_steps/synthetic_targets.rs
         || env::var("RUSTC_BOOTSTRAP_SYNTHETIC_TARGET").is_ok()
diff --git rust-clean/library/std/src/os/mod.rs rust-workdir/library/std/src/os/mod.rs
index 7637440..5b5a4b3 100644
--- rust-clean/library/std/src/os/mod.rs
+++ rust-workdir/library/std/src/os/mod.rs
@@ -155,6 +155,8 @@ pub mod ios;
 pub mod l4re;
 #[cfg(target_os = "macos")]
 pub mod macos;
+#[cfg(target_os = "zinnia")]
+pub mod zinnia;
 #[cfg(target_os = "motor")]
 pub mod motor;
 #[cfg(target_os = "netbsd")]
diff --git rust-clean/library/std/src/os/unix/mod.rs rust-workdir/library/std/src/os/unix/mod.rs
index 78c9572..6c11992 100644
--- rust-clean/library/std/src/os/unix/mod.rs
+++ rust-workdir/library/std/src/os/unix/mod.rs
@@ -67,6 +67,8 @@ mod platform {
     pub use crate::os::l4re::*;
     #[cfg(target_os = "linux")]
     pub use crate::os::linux::*;
+    #[cfg(target_os = "zinnia")]
+    pub use crate::os::zinnia::*;
     #[cfg(target_os = "netbsd")]
     pub use crate::os::netbsd::*;
     #[cfg(target_os = "nto")]
diff --git rust-workdir/library/std/src/os/zinnia/fs.rs rust-workdir/library/std/src/os/zinnia/fs.rs
new file mode 100644
index 0000000..8cded1d
--- /dev/null
+++ rust-workdir/library/std/src/os/zinnia/fs.rs
@@ -0,0 +1,95 @@
+#![stable(feature = "metadata_ext", since = "1.1.0")]
+
+use crate::fs::Metadata;
+use crate::sys::AsInner;
+
+/// OS-specific extensions to [`fs::Metadata`].
+///
+/// [`fs::Metadata`]: crate::fs::Metadata
+#[stable(feature = "metadata_ext", since = "1.1.0")]
+pub trait MetadataExt {
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_dev(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_ino(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_mode(&self) -> u32;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_nlink(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_uid(&self) -> u32;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_gid(&self) -> u32;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_rdev(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_size(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_atime(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_atime_nsec(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_mtime(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_mtime_nsec(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_ctime(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_ctime_nsec(&self) -> i64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_blksize(&self) -> u64;
+    #[stable(feature = "metadata_ext2", since = "1.8.0")]
+    fn st_blocks(&self) -> u64;
+}
+
+#[stable(feature = "metadata_ext", since = "1.1.0")]
+impl MetadataExt for Metadata {
+    fn st_dev(&self) -> u64 {
+        self.as_inner().as_inner().st_dev as u64
+    }
+    fn st_ino(&self) -> u64 {
+        self.as_inner().as_inner().st_ino as u64
+    }
+    fn st_mode(&self) -> u32 {
+        self.as_inner().as_inner().st_mode as u32
+    }
+    fn st_nlink(&self) -> u64 {
+        self.as_inner().as_inner().st_nlink as u64
+    }
+    fn st_uid(&self) -> u32 {
+        self.as_inner().as_inner().st_uid as u32
+    }
+    fn st_gid(&self) -> u32 {
+        self.as_inner().as_inner().st_gid as u32
+    }
+    fn st_rdev(&self) -> u64 {
+        self.as_inner().as_inner().st_rdev as u64
+    }
+    fn st_size(&self) -> u64 {
+        self.as_inner().as_inner().st_size as u64
+    }
+    fn st_atime(&self) -> i64 {
+        self.as_inner().as_inner().st_atime as i64
+    }
+    fn st_atime_nsec(&self) -> i64 {
+        self.as_inner().as_inner().st_atime_nsec as i64
+    }
+    fn st_mtime(&self) -> i64 {
+        self.as_inner().as_inner().st_mtime as i64
+    }
+    fn st_mtime_nsec(&self) -> i64 {
+        self.as_inner().as_inner().st_mtime_nsec as i64
+    }
+    fn st_ctime(&self) -> i64 {
+        self.as_inner().as_inner().st_ctime as i64
+    }
+    fn st_ctime_nsec(&self) -> i64 {
+        self.as_inner().as_inner().st_ctime_nsec as i64
+    }
+    fn st_blksize(&self) -> u64 {
+        self.as_inner().as_inner().st_blksize as u64
+    }
+    fn st_blocks(&self) -> u64 {
+        self.as_inner().as_inner().st_blocks as u64
+    }
+}
diff --git rust-workdir/library/std/src/os/zinnia/mod.rs rust-workdir/library/std/src/os/zinnia/mod.rs
new file mode 100644
index 0000000..e02ba8f
--- /dev/null
+++ rust-workdir/library/std/src/os/zinnia/mod.rs
@@ -0,0 +1,4 @@
+#![stable(feature = "raw_ext", since = "1.1.0")]
+
+pub mod fs;
+pub mod raw;
diff --git rust-workdir/library/std/src/os/zinnia/raw.rs rust-workdir/library/std/src/os/zinnia/raw.rs
new file mode 100644
index 0000000..a811c42
--- /dev/null
+++ rust-workdir/library/std/src/os/zinnia/raw.rs
@@ -0,0 +1,74 @@
+#![stable(feature = "raw_ext", since = "1.1.0")]
+#![deprecated(
+    since = "1.8.0",
+    note = "these type aliases are no longer supported by \
+            the standard library, the `libc` crate on \
+            crates.io should be used instead for the correct \
+            definitions"
+)]
+#![allow(deprecated)]
+
+use crate::os::raw::{c_long, c_void};
+
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type dev_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type gid_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type mode_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type uid_t = usize;
+
+#[stable(feature = "pthread_t", since = "1.8.0")]
+pub type pthread_t = *mut c_void;
+
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type blkcnt_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type blksize_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type ino_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type nlink_t = usize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type off_t = isize;
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub type time_t = usize;
+
+#[repr(C)]
+#[derive(Clone)]
+#[stable(feature = "raw_ext", since = "1.1.0")]
+pub struct stat {
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_dev: dev_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_ino: ino_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_mode: mode_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_nlink: nlink_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_uid: uid_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_gid: gid_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_rdev: dev_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_size: off_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_atime: time_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_atime_nsec: c_long,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_mtime: time_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_mtime_nsec: c_long,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_ctime: time_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_ctime_nsec: c_long,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_blksize: blksize_t,
+    #[stable(feature = "raw_ext", since = "1.1.0")]
+    pub st_blocks: blkcnt_t,
+}
diff --git rust-clean/library/std/src/sys/args/unix.rs rust-workdir/library/std/src/sys/args/unix.rs
index 7a592c2..5196b88 100644
--- rust-clean/library/std/src/sys/args/unix.rs
+++ rust-workdir/library/std/src/sys/args/unix.rs
@@ -84,6 +84,7 @@ pub fn args() -> Args {
     target_os = "hurd",
     target_os = "rtems",
     target_os = "nuttx",
+    target_os = "zinnia"
 ))]
 mod imp {
     use crate::ffi::c_char;
diff --git rust-clean/library/std/src/sys/env_consts.rs rust-workdir/library/std/src/sys/env_consts.rs
index 573f540..fc6b192 100644
--- rust-clean/library/std/src/sys/env_consts.rs
+++ rust-workdir/library/std/src/sys/env_consts.rs
@@ -147,6 +147,17 @@ pub mod os {
     pub const EXE_EXTENSION: &str = "";
 }
 
+#[cfg(target_os = "zinnia")]
+pub mod os {
+    pub const FAMILY: &str = "unix";
+    pub const OS: &str = "zinnia";
+    pub const DLL_PREFIX: &str = "lib";
+    pub const DLL_SUFFIX: &str = ".so";
+    pub const DLL_EXTENSION: &str = "so";
+    pub const EXE_SUFFIX: &str = "";
+    pub const EXE_EXTENSION: &str = "";
+}
+
 #[cfg(target_os = "illumos")]
 pub mod os {
     pub const FAMILY: &str = "unix";
diff --git rust-clean/library/std/src/sys/fd/unix.rs rust-workdir/library/std/src/sys/fd/unix.rs
index bb6c0ac..b4e27d9 100644
--- rust-clean/library/std/src/sys/fd/unix.rs
+++ rust-workdir/library/std/src/sys/fd/unix.rs
@@ -66,6 +66,7 @@ const READ_LIMIT: usize = if cfg!(target_vendor = "apple") {
     target_os = "openbsd",
     target_vendor = "apple",
     target_os = "cygwin",
+    target_os = "zinnia",
 ))]
 const fn max_iov() -> usize {
     libc::IOV_MAX as usize
@@ -96,6 +97,7 @@ const fn max_iov() -> usize {
     target_os = "vita",
     target_vendor = "apple",
     target_os = "cygwin",
+    target_os = "zinnia",
 )))]
 const fn max_iov() -> usize {
     16 // The minimum value required by POSIX.
@@ -219,6 +221,7 @@ impl FileDesc {
         target_os = "linux",
         target_os = "netbsd",
         target_os = "openbsd", // OpenBSD 2.7
+        target_os = "zinnia",
     ))]
     pub fn read_vectored_at(&self, bufs: &mut [IoSliceMut<'_>], offset: u64) -> io::Result<usize> {
         let ret = cvt(unsafe {
@@ -245,6 +248,7 @@ impl FileDesc {
         target_os = "netbsd",
         target_os = "openbsd",
         target_vendor = "apple",
+        target_os = "zinnia",
     )))]
     pub fn read_vectored_at(&self, bufs: &mut [IoSliceMut<'_>], offset: u64) -> io::Result<usize> {
         io::default_read_vectored(|b| self.read_at(b, offset), bufs)
@@ -426,6 +430,7 @@ impl FileDesc {
         target_os = "linux",
         target_os = "netbsd",
         target_os = "openbsd", // OpenBSD 2.7
+        target_os = "zinnia",
     ))]
     pub fn write_vectored_at(&self, bufs: &[IoSlice<'_>], offset: u64) -> io::Result<usize> {
         let ret = cvt(unsafe {
@@ -452,6 +457,7 @@ impl FileDesc {
         target_os = "netbsd",
         target_os = "openbsd",
         target_vendor = "apple",
+        target_os = "zinnia",
     )))]
     pub fn write_vectored_at(&self, bufs: &[IoSlice<'_>], offset: u64) -> io::Result<usize> {
         io::default_write_vectored(|b| self.write_at(b, offset), bufs)
@@ -562,6 +568,7 @@ impl FileDesc {
         target_os = "vxworks",
         target_os = "nto",
         target_os = "wasi",
+        target_os = "zinnia",
     )))]
     pub fn set_cloexec(&self) -> io::Result<()> {
         unsafe {
@@ -586,6 +593,7 @@ impl FileDesc {
         target_os = "vxworks",
         target_os = "nto",
         target_os = "wasi",
+        target_os = "zinnia",
     ))]
     pub fn set_cloexec(&self) -> io::Result<()> {
         unsafe {
diff --git rust-clean/library/std/src/sys/fs/unix.rs rust-workdir/library/std/src/sys/fs/unix.rs
index 7db4745..caa63dc 100644
--- rust-clean/library/std/src/sys/fs/unix.rs
+++ rust-workdir/library/std/src/sys/fs/unix.rs
@@ -1142,6 +1142,7 @@ impl DirEntry {
         target_os = "vita",
         target_os = "vxworks",
         target_os = "wasi",
+        target_os = "zinnia",
         target_vendor = "apple",
     ))]
     pub fn ino(&self) -> u64 {
diff --git rust-clean/library/std/src/sys/io/error/unix.rs rust-workdir/library/std/src/sys/io/error/unix.rs
index b10343b..3973f19 100644
--- rust-clean/library/std/src/sys/io/error/unix.rs
+++ rust-workdir/library/std/src/sys/io/error/unix.rs
@@ -10,6 +10,7 @@ unsafe extern "C" {
             target_os = "fuchsia",
             target_os = "l4re",
             target_os = "hurd",
+            target_os = "zinnia",
         ),
         link_name = "__errno_location"
     )]
diff --git rust-clean/library/std/src/sys/net/connection/socket/unix.rs rust-workdir/library/std/src/sys/net/connection/socket/unix.rs
index 5e20c0f..71efedf 100644
--- rust-clean/library/std/src/sys/net/connection/socket/unix.rs
+++ rust-workdir/library/std/src/sys/net/connection/socket/unix.rs
@@ -77,6 +77,7 @@ impl Socket {
                 target_os = "cygwin",
                 target_os = "nto",
                 target_os = "solaris",
+                target_os = "zinnia",
             ) => {
                 // On platforms that support it we pass the SOCK_CLOEXEC
                 // flag to atomically create the socket and set it as
@@ -124,6 +125,7 @@ impl Socket {
                     target_os = "openbsd",
                     target_os = "cygwin",
                     target_os = "nto",
+                    target_os = "zinnia",
                 ) => {
                     // Like above, set cloexec atomically
                     cvt(libc::socketpair(fam, ty | libc::SOCK_CLOEXEC, 0, fds.as_mut_ptr()))?;
@@ -254,6 +256,7 @@ impl Socket {
                 target_os = "netbsd",
                 target_os = "openbsd",
                 target_os = "cygwin",
+                target_os = "zinnia",
             ) => {
                 unsafe {
                     let fd = cvt_r(|| libc::accept4(self.as_raw_fd(), storage, len, libc::SOCK_CLOEXEC))?;
diff --git rust-clean/library/std/src/sys/pal/unix/os.rs rust-workdir/library/std/src/sys/pal/unix/os.rs
index 494d944..e25d582 100644
--- rust-clean/library/std/src/sys/pal/unix/os.rs
+++ rust-workdir/library/std/src/sys/pal/unix/os.rs
@@ -360,6 +360,12 @@ pub fn current_exe() -> io::Result<PathBuf> {
     Err(io::const_error!(io::ErrorKind::Unsupported, "not yet implemented!"))
 }
 
+#[cfg(target_os = "zinnia")]
+pub fn current_exe() -> io::Result<PathBuf> {
+    use crate::io::ErrorKind;
+    Err(io::const_error!(ErrorKind::Unsupported, "not yet implemented!"))
+}
+
 #[cfg(target_os = "vxworks")]
 pub fn current_exe() -> io::Result<PathBuf> {
     #[cfg(test)]
diff --git rust-clean/library/std/src/sys/random/mod.rs rust-workdir/library/std/src/sys/random/mod.rs
index 91f72d0..2ee1110 100644
--- rust-clean/library/std/src/sys/random/mod.rs
+++ rust-workdir/library/std/src/sys/random/mod.rs
@@ -62,6 +62,10 @@ cfg_select! {
         mod redox;
         pub use redox::fill_bytes;
     }
+    target_os = "zinnia" => {
+        mod getentropy;
+        pub use getentropy::fill_bytes;
+    }
     target_os = "motor" => {
         mod motor;
         pub use motor::fill_bytes;
diff --git rust-clean/library/std/src/sys/thread/unix.rs rust-workdir/library/std/src/sys/thread/unix.rs
index 22f9bfe..9f5330d 100644
--- rust-clean/library/std/src/sys/thread/unix.rs
+++ rust-workdir/library/std/src/sys/thread/unix.rs
@@ -155,6 +155,7 @@ pub fn available_parallelism() -> io::Result<NonZero<usize>> {
             target_os = "aix",
             target_vendor = "apple",
             target_os = "cygwin",
+            target_os = "zinnia",
         ) => {
             #[allow(unused_assignments)]
             #[allow(unused_mut)]
@@ -420,7 +421,8 @@ pub fn set_name(name: &CStr) {
     target_os = "freebsd",
     target_os = "dragonfly",
     target_os = "nuttx",
-    target_os = "cygwin"
+    target_os = "cygwin",
+    target_os = "zinnia"
 ))]
 pub fn set_name(name: &CStr) {
     unsafe {
diff --git rust-clean/library/unwind/src/lib.rs rust-workdir/library/unwind/src/lib.rs
index cce6ca7..da5eda0 100644
--- rust-clean/library/unwind/src/lib.rs
+++ rust-workdir/library/unwind/src/lib.rs
@@ -163,6 +163,11 @@ unsafe extern "C" {}
 #[link(name = "gcc_s")]
 unsafe extern "C" {}
 
+#[cfg(target_os = "zinnia")]
+#[link(name = "gcc_eh", kind = "static", modifiers = "-bundle", cfg(target_feature = "crt-static"))]
+#[link(name = "gcc_s", cfg(not(target_feature = "crt-static")))]
+unsafe extern "C" {}
+
 #[cfg(target_os = "freebsd")]
 #[link(name = "gcc", kind = "static", modifiers = "-bundle", cfg(target_feature = "crt-static"))]
 #[link(name = "gcc_eh", kind = "static", modifiers = "-bundle", cfg(target_feature = "crt-static"))]
diff --git rust-clean/src/tools/generate-copyright/src/cargo_metadata.rs rust-workdir/src/tools/generate-copyright/src/cargo_metadata.rs
index 87cd85c..1eab545 100644
--- rust-clean/src/tools/generate-copyright/src/cargo_metadata.rs
+++ rust-workdir/src/tools/generate-copyright/src/cargo_metadata.rs
@@ -77,11 +77,22 @@ pub fn get_metadata(
         if manifest_path.file_name() != Some(OsStr::new("Cargo.toml")) {
             panic!("cargo_manifest::get requires a path to a Cargo.toml file");
         }
-        let metadata = cargo_metadata::MetadataCommand::new()
+        let metadata = match cargo_metadata::MetadataCommand::new()
             .cargo_path(cargo)
             .env("RUSTC_BOOTSTRAP", "1")
             .manifest_path(manifest_path)
-            .exec()?;
+            .exec()
+        {
+            Ok(metadata) => metadata,
+            Err(e) if is_non_installed_example_manifest(root_path, manifest_path) => {
+                eprintln!(
+                    "Skipping optional manifest {:?} while collecting copyright metadata: {e}",
+                    manifest_path
+                );
+                continue;
+            }
+            Err(e) => return Err(e.into()),
+        };
         for package in metadata.packages {
             let package_manifest_path = package.manifest_path.as_path();
 
@@ -110,6 +121,14 @@ pub fn get_metadata(
     Ok(output)
 }
 
+fn is_non_installed_example_manifest(root_path: &Path, manifest_path: &Path) -> bool {
+    let Ok(relative) = manifest_path.strip_prefix(root_path) else {
+        return false;
+    };
+
+    relative.starts_with("src/doc") || relative.starts_with("tests")
+}
+
 /// Add important files off disk into this dependency.
 ///
 /// Maybe one-day Cargo.toml will contain enough information that we don't need
@@ -121,7 +140,12 @@ fn load_important_files(
     let name_version = format!("{}-{}", package.name, package.version);
     println!("Scraping notices for {}...", name_version);
     let dep_vendor_path = vendor_root.join(name_version);
-    for entry in std::fs::read_dir(dep_vendor_path)? {
+    let dep_vendor_entries = match std::fs::read_dir(&dep_vendor_path) {
+        Ok(entries) => entries,
+        Err(e) if e.kind() == std::io::ErrorKind::NotFound => return Ok(()),
+        Err(e) => return Err(e.into()),
+    };
+    for entry in dep_vendor_entries {
         let entry = entry?;
         let metadata = entry.metadata()?;
         let path = entry.path();
