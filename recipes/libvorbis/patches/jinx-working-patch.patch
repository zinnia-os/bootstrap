diff --git libvorbis-clean/CMakeLists.txt libvorbis-workdir/CMakeLists.txt
index f377c42..5dd3d28 100644
--- libvorbis-clean/CMakeLists.txt
+++ libvorbis-workdir/CMakeLists.txt
@@ -42,6 +42,7 @@ function(get_version_info result current_var_name age_var_name revision_var_name
     math(EXPR VERSION_INFO_CURRENT_MINUS_AGE "${VERSION_INFO_CURRENT} - ${VERSION_INFO_AGE}")
 
     set(${result} "${VERSION_INFO_CURRENT_MINUS_AGE}.${VERSION_INFO_AGE}.${VERSION_INFO_REVISION}" PARENT_SCOPE)
+    set(${result}_SOVERSION "${VERSION_INFO_CURRENT_MINUS_AGE}" PARENT_SCOPE)
 endfunction()
 
 # Helper function to configure pkg-config files
diff --git libvorbis-clean/lib/CMakeLists.txt libvorbis-workdir/lib/CMakeLists.txt
index 7cd68e5..c4520f6 100644
--- libvorbis-clean/lib/CMakeLists.txt
+++ libvorbis-workdir/lib/CMakeLists.txt
@@ -79,11 +79,14 @@ if (NOT BUILD_FRAMEWORK)
     add_library(vorbisfile ${VORBISFILE_SOURCES})
 
     get_version_info(VORBIS_VERSION_INFO "V_LIB_CURRENT" "V_LIB_AGE" "V_LIB_REVISION")
-    set_target_properties(vorbis PROPERTIES SOVERSION ${VORBIS_VERSION_INFO})
+    set_target_properties(vorbis PROPERTIES
+        VERSION ${VORBIS_VERSION_INFO} SOVERSION ${VORBIS_VERSION_INFO_SOVERSION})
     get_version_info(VORBISENC_VERSION_INFO "VE_LIB_CURRENT" "VE_LIB_AGE" "VE_LIB_REVISION")
-    set_target_properties(vorbisenc PROPERTIES SOVERSION ${VORBISENC_VERSION_INFO})
+    set_target_properties(vorbisenc PROPERTIES
+        VERSION ${VORBISENC_VERSION_INFO} SOVERSION ${VORBISENC_VERSION_INFO_SOVERSION})
     get_version_info(VORBISFILE_VERSION_INFO "VF_LIB_CURRENT" "VF_LIB_AGE" "VF_LIB_REVISION")
-    set_target_properties(vorbisfile PROPERTIES SOVERSION ${VORBISFILE_VERSION_INFO})
+    set_target_properties(vorbisfile PROPERTIES
+        VERSION ${VORBISFILE_VERSION_INFO} SOVERSION ${VORBISFILE_VERSION_INFO_SOVERSION})
 
     target_include_directories(vorbis
         PUBLIC
