diff --git seatd-clean/common/drm.c seatd-workdir/common/drm.c
index 9591dc0..8096512 100644
--- seatd-clean/common/drm.c
+++ seatd-workdir/common/drm.c
@@ -34,7 +34,7 @@ int path_is_drm(const char *path) {
 int dev_is_drm(dev_t device) {
 	return major(device) == 226;
 }
-#elif defined(__FreeBSD__)
+#elif defined(__FreeBSD__) || defined(__zinnia__)
 int path_is_drm(const char *path) {
 	static const char prefix[] = "/dev/drm/";
 	static const int prefixlen = STRLEN(prefix);
diff --git seatd-clean/common/evdev.c seatd-workdir/common/evdev.c
index 4aff9bc..f3dd4fe 100644
--- seatd-clean/common/evdev.c
+++ seatd-workdir/common/evdev.c
@@ -9,6 +9,8 @@
 #include <sys/sysmacros.h>
 #elif defined(__FreeBSD__)
 #include <dev/evdev/input.h>
+#elif defined(__zinnia__)
+#include <linux/input.h>
 #else
 #error Unsupported platform
 #endif
diff --git seatd-clean/common/terminal.c seatd-workdir/common/terminal.c
index 0c3466f..678f63f 100644
--- seatd-clean/common/terminal.c
+++ seatd-workdir/common/terminal.c
@@ -21,6 +21,37 @@
 #define K_ENABLE  K_XLATE
 #define K_DISABLE K_RAW
 #define FRSIG     SIGIO
+#elif defined(__zinnia__)
+#define KDSETMODE   0x4B3A
+#define KDSKBMODE   0x4B45
+#define KD_TEXT     0x00
+#define KD_GRAPHICS 0x01
+#define K_RAW       0x00
+#define K_XLATE     0x01
+#define K_UNICODE   0x03
+#define K_OFF       0x04
+#define VT_SETMODE  0x5602
+#define VT_GETSTATE 0x5603
+#define VT_RELDISP  0x5605
+#define VT_ACTIVATE 0x5606
+#define VT_AUTO     0x00
+#define VT_PROCESS  0x01
+#define VT_ACKACQ   0x02
+struct vt_mode {
+	char mode;
+	char waitv;
+	short relsig;
+	short acqsig;
+	short frsig;
+};
+struct vt_stat {
+	unsigned short v_active;
+	unsigned short v_signal;
+	unsigned short v_state;
+};
+#define K_ENABLE  K_UNICODE
+#define K_DISABLE K_OFF
+#define FRSIG     0
 #else
 #error Unsupported platform
 #endif
@@ -126,7 +157,7 @@ static int get_tty_path(int tty, char path[static TTYPATHLEN]) {
 	path[offset++] = '\0';
 	return 0;
 }
-#elif defined(__linux__)
+#elif defined(__linux__) || defined(__zinnia__)
 static int get_tty_path(int tty, char path[static TTYPATHLEN]) {
 	assert(tty >= 0);
 	if (snprintf(path, TTYPATHLEN, "/dev/tty%d", tty) == -1) {
@@ -153,7 +184,7 @@ int terminal_open(int vt) {
 }
 
 int terminal_current_vt(int fd) {
-#if defined(__linux__)
+#if defined(__linux__) || defined(__zinnia__)
 	struct vt_stat st;
 	int res = ioctl(fd, VT_GETSTATE, &st);
 	close(fd);
@@ -196,6 +227,7 @@ int terminal_set_process_switching(int fd, bool enable) {
 			   enable ? "enable" : "disable", strerror(errno));
 		return -1;
 	}
+	(void)fd;
 	return 0;
 }
 
@@ -205,7 +237,7 @@ int terminal_switch_vt(int fd, int vt) {
 		log_errorf("Could not activate VT %d: %s", vt, strerror(errno));
 		return -1;
 	}
-
+	(void)fd;
 	return 0;
 }
 
@@ -215,7 +247,7 @@ int terminal_ack_release(int fd) {
 		log_errorf("Could not ack VT release: %s", strerror(errno));
 		return -1;
 	}
-
+	(void)fd;
 	return 0;
 }
 
@@ -225,7 +257,7 @@ int terminal_ack_acquire(int fd) {
 		log_errorf("Could not ack VT acquire: %s", strerror(errno));
 		return -1;
 	}
-
+	(void)fd;
 	return 0;
 }
 
@@ -253,6 +285,7 @@ int terminal_set_keyboard(int fd, bool enable) {
 		return -1;
 	}
 #endif
+	(void)fd;
 	return 0;
 }
 
@@ -263,5 +296,6 @@ int terminal_set_graphics(int fd, bool enable) {
 			   strerror(errno));
 		return -1;
 	}
+	(void)fd;
 	return 0;
 }
diff --git seatd-clean/seatd/client.c seatd-workdir/seatd/client.c
index 220c5d3..df57c42 100644
--- seatd-clean/seatd/client.c
+++ seatd-workdir/seatd/client.c
@@ -24,7 +24,7 @@
 #include "terminal.h"
 
 static int get_peer(int fd, pid_t *pid, uid_t *uid, gid_t *gid) {
-#if defined(__linux__)
+#if defined(__linux__) || defined(__zinnia__)
 	struct ucred cred;
 	socklen_t len = sizeof cred;
 	if (getsockopt(fd, SOL_SOCKET, SO_PEERCRED, &cred, &len) == -1) {
