type = process
command = /bin/sh -c "mkdir -p /run/lightdm; XDG_RUNTIME_DIR=/tmp exec /usr/bin/lightdm --debug >/dev/com1 2>&1"
depends-on = dbus
depends-on = seatd
depends-on = devd
depends-on = fake-logind
working-dir = /root
restart = yes
